<Window x:Class="GrammarFixer.AddLanguageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Custom Language"
        Height="300"
        Width="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Border CornerRadius="12" Background="#14141E" BorderBrush="#3C3C50" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#1E1E2D" CornerRadius="12,12,0,0">
                <Grid>
                    <TextBlock Text="Add Custom Language" 
                               Foreground="White" 
                               FontSize="16" 
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"/>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Margin="25" VerticalAlignment="Center">
                <TextBlock Text="Language Name:" Foreground="#B4B4BE" FontSize="12" Margin="0,0,0,8"/>
                <TextBox x:Name="LanguageNameBox"
                         Height="40"
                         Style="{StaticResource ModernTextBox}"/>
            </StackPanel>

            <Border Grid.Row="2" Background="#1E1E2D" CornerRadius="0,0,12,12" Padding="25">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Add"
                            x:Name="AddButton"
                            Click="AddButton_Click"
                            IsDefault="True"
                            Style="{StaticResource ModernButton}"
                            Width="100"
                            Height="40"
                            Margin="0,0,10,0"/>
                    <Button Content="Cancel"
                            IsCancel="True"
                            Style="{StaticResource ModernButton}"
                            Background="#2D2D42"
                            Width="100"
                            Height="40"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>